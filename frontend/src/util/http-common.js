const IP ="http://j3d104.p.ssafy.io"
export default{
    BASE_URL: `${IP}:8080`,
    IMG_URL: `${IP}:3000`,
    JSON_HEADER:{
        headers: {
            'Content-Type':'application/json'
        }
    },
    
}