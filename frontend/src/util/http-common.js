const IP ="http://192.168.199"
export default{
    BASE_URL: `${IP}:8090`,
    IMG_URL: `${IP}:3000`,
    JSON_HEADER:{
        headers: {
            'Content-Type':'application/json'
        }
    },
    
}