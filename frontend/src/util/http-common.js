const IP ="http://192.168.219.137"
export default{
    BASE_URL: `${IP}:8080`,
    IMG_URL: `${IP}:3000`,
    JSON_HEADER:{
        headers: {
            'Content-Type':'application/json'
        }
    },
    
}