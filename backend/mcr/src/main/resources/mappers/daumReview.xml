<?xml version="1.0" encoding="UTF-8"?>
<!-- mappers/gueset.xml -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.mcr.dao.DaumReviewDao">

	<!-- insert 처리 -->
	<insert id="insertDaumReview" parameterType="DaumReview">
		insert into daum_review( 
						movie_id,
						writer,
						rate,
						content,
						regtime
				)
				select	#{movie_id},
						#{writer},
						#{rate},
						#{content},
						#{regtime}
				FROM dual
            WHERE NOT EXISTS (
                        SELECT movie_id,
						writer,
						rate,
						content,
						regtime
                 FROM daum_review
                 WHERE movie_id = #{movie_id}
                 	AND writer = #{writer}
                 	AND regtime = #{regtime}
            )
	</insert>
	
	<!-- id으로 select 처리 -->
	<select id="selectDaumReviewsByMovieId" parameterType="Integer" resultType="DaumReview">
		select 
			* 
		  from 
		  	daum_review 
		 where 
		 	movie_id = #{movie_id}
	</select>
</mapper>

